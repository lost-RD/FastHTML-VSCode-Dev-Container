# FastHTML Dev Environment

To run scripts at startup, make a folder `user` and add `user.sh` into it. It will be executed in a separate process.

Use this to install Python modules, VSCode extensions, etc.
